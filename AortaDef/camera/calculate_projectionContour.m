function [contour, contour_index] = calculate_projectionContour(vertex_proj, radius)
%calculate projection contour using projection vertices




% todo: calculate projection contour



contour_index = boundary_index;
contour = vertex_proj(contour_index,1:2);        % contour of model

end